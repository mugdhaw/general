Windows commands : 
 1) git clone 'httpsUrl' : It clones the repository to your local machine
 2) git config --list : To list all the settings Git can find at that point:
 3) git add :
 4) Git commit
 5) git push origin master (branch)
 6) Update the configuration globallay as following example : 
    git config --global user.name "Mugdha W"
     git config --global user.email mugdha@gmail.com
* Generate public key using ssh-key gen if permission denied during clone using ssh
